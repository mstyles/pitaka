<!-- What changes for the user, and why. -->

## Checks

- [ ] `cargo fmt --all` and `cargo clippy --workspace --all-targets` (warning-free)
- [ ] `cargo test -p ebook_research_core`
- [ ] `npx tsc --noEmit` and `npm test`
- [ ] README updated: what's verified, known limitations, roadmap

## What was verified, and what wasn't

<!-- e.g. "checked in the browser against mocks, not clicked through in
the Tauri window". -->
